#version 430 core

layout (local_size_x = 16, local_size_y = 16) in;

layout (rg32f, binding = 4) readonly uniform image2D NoiseRead;
layout (rg32f, binding = 5) writeonly uniform image2D NoiseWrite;
layout (r8ui, binding = 6) readonly uniform uimage2D ConstraintMaskImage;
void main(){

}